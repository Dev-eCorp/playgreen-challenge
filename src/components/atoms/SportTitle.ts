import styled from "styled-components";

const SportTitle = styled.p`
  position: absolute;
  left: 24px;
  font-family: "DM Sans";
  font-style: normal;
  font-weight: 700;
  font-size: 24px;
  color: #fefefe;
  z-index: 2;
`;

export default SportTitle;
