import styled from "styled-components";

const ImageContainer = styled.div`
  position: relative;
  width: 80%;
  height: 100%;
  border-radius: 12px;
  overflow: hidden;
`;

export default ImageContainer;
